# Netspeak Portal — Development Timeline

This timeline records each development task, the time consumed, and specific, direct-to-the-point changes and updates.

| Task / Update | Time Consumed | Changes and Updates |
| :--- | :--- | :--- |
| **Phase 0: Project Discovery & Specification Review** | 15 mins | • Read and analyzed `Netspeak-System-Specification.md` to identify core architecture and module requirements.<br>• Verified Node.js (`v22.20.0`) and npm (`11.10.1`) versions on local machine.<br>• Inspected repository workspace structure (clean starting point with spec file).<br>• Confirmed non-Supabase-Auth constraint: custom session authentication with HTTP-only cookies and PostgreSQL backend. |
| **Milestone Planning & Documentation Setup** | 10 mins | • Created implementation plan for Milestone 1 (Core Application Foundation) detailing app shell, Prisma database schema, session auth, RBAC authorization, and user management.<br>• Established tracking files: `docs/development-timeline.md`, `docs/implementation-status.md`, and `docs/development-notes.md`. |
| **Core Architecture & Schema Definition** | 20 mins | • Configured `package.json`, `tsconfig.json`, `next.config.js`, `.env.example`, `.env`, and `.gitignore`.<br>• Defined Prisma schema (`prisma/schema.prisma`) for core entities: `User`, `Role`, `Permission`, `UserRole`, `RolePermission`, `Session`.<br>• Created DB client singleton (`src/lib/db.ts`). |
| **Authentication & RBAC Implementation** | 25 mins | • Built password hashing and secure token generator (`src/lib/auth/password.ts`) using bcrypt.<br>• Built session management service (`src/lib/auth/session.ts`) handling secure HTTP-only cookies and database verification.<br>• Implemented server-side RBAC guards and permission checkers (`src/lib/auth/rbac.ts`).<br>• Created Server Actions for Login and Logout (`src/actions/auth.ts`). |
| **App Shell & User Management UI** | 25 mins | • Built base design system stylesheet (`src/app/globals.css`).<br>• Created RootLayout, Loading (`loading.tsx`), Error (`error.tsx`), and Not-Found (`not-found.tsx`) shells.<br>• Created `/login` page with client-side interactive `LoginForm` component.<br>• Created AppHeader, AppSidebar, and authenticated `/dashboard` layout.<br>• Built user creation and activation toggle Server Actions (`src/actions/users.ts`) with permission enforcement.<br>• Built interactive `UserManagementSection` component and database seeding script (`prisma/seed.ts`). |
| **Prisma & Package Dependencies Sync** | 10 mins | • Installed Prisma 5 (`@prisma/client@^5.22.0`, `prisma@^5.22.0`) & `tsx`.<br>• Resolved ESM package type alignment in `package.json` and `next.config.js`. |
| **Verification & Production Build** | 10 mins | • TypeScript check passed with 0 errors (`npx tsc --noEmit`).<br>• Next.js production build succeeded (`npm run build`).<br>• Next.js development server active and responding with HTTP 200 on `http://localhost:3000/login`.<br>• Unauthenticated requests to `/dashboard` successfully guarded with redirect to `/login`. |
| **Supabase PostgreSQL Integration** | 15 mins | • URL-encoded password special characters in `DATABASE_URL` and `DIRECT_URL`.<br>• Fixed duplicate `DATABASE_URL` in `.env` file.<br>• Switched Prisma schema from SQLite to PostgreSQL with `directUrl` for migrations.<br>• `npx prisma db push` — schema applied to Supabase (tables: `User`, `Role`, `Permission`, `UserRole`, `RolePermission`, `Session`).<br>• `npx tsx prisma/seed.ts` — seeded 6 roles, 7 permissions, and initial `admin` account in Supabase.<br>• Dev server restarted and confirmed running on `http://localhost:3000`. |
| **Milestone 2: Schema & Database Push** | 15 mins | • Extended `prisma/schema.prisma` with `ProjectType`, `Department`, and `RegistrationStatus` enums.<br>• Added `TeacherProfile` model with personal, academic, operational, and approval workflow fields.<br>• Synchronized schema to Supabase PostgreSQL (`npx prisma db push`).<br>• Added teacher management permissions (`teachers:read`, `teachers:create`, `teachers:update`, `teachers:review`, `teachers:approve`) to `rbac.ts` and `prisma/seed.ts`.<br>• Executed `npx tsx prisma/seed.ts` to assign appropriate review/approve authorities to Admin and Operations Manager roles. |
| **Milestone 2: Server Actions & Deduplication** | 20 mins | • Built `registerTeacherAction` with Zod validation and strict deduplication against existing profiles by (Real Full Name + Birthday) OR (Cellphone Number).<br>• Built multi-stage approval actions: `reviewTeacherRegistrationAction` (marks `UNDER_REVIEW`), `approveTeacherRegistrationAction` (creates linked `User`, assigns `TEACHER` role, generates secure temporary credentials, marks `APPROVED`), and `rejectTeacherRegistrationAction` (records mandatory rationale).<br>• Built `updateTeacherOperationalAction` for editing launch dates, department, project type, rest days, and 51Talk portal credentials. |
| **Milestone 2: Teacher UI & Workflow Pages** | 25 mins | • Created public teacher registration page (`/register`) with interactive multi-step component (`TeacherRegistrationForm.tsx`).<br>• Created Teacher Management directory page (`/dashboard/teachers`) with KPI metrics and filterable table (`TeacherDirectoryTable.tsx`).<br>• Created comprehensive Teacher Profile Detail view (`/dashboard/teachers/[id]`) with role-based workflow triggers and operational editor (`TeacherProfileDetail.tsx`).<br>• Integrated navigation links into `AppSidebar.tsx`. |
| **Milestone 2: Verification & Production Build** | 10 mins | • TypeScript check passed with 0 errors (`npx tsc --noEmit`).<br>• Full Next.js production build succeeded (`npm run build`).<br>• Dev server active on `http://localhost:3000` with `/register` responding HTTP 200. |
| **Milestone 3: Attendance Schema & Supabase Sync** | 15 mins | • Added `ShiftSchedule` model, `TeacherAttendance` model, and `AttendanceStatus` enum to `prisma/schema.prisma`.<br>• Synchronized schema to Supabase PostgreSQL (`npx prisma db push`).<br>• Extended `rbac.ts` and `seed.ts` with `attendance:read`, `attendance:record`, `attendance:verify`, and `attendance:manage` permissions.<br>• Seeded initial standard shifts ("Morning Shift 08:00 - 17:00" and "Afternoon Shift 13:00 - 22:00") and role assignments. |
| **Milestone 3: Shift Rules & Attendance Server Actions** | 20 mins | • Built `recordFreshnessCheckAction` enforcing T-30 early login rule (cannot log in prior to 30 mins before shift) and T-0 tardiness evaluation (evaluating late minutes against scheduled shift).<br>• Built `recordTeacherLogoutAction` with active shift lockdown (restricting logout during active hours before T-15 of shift end, requiring early-out rationale).<br>• Built `verifyTeacherAttendanceAction` allowing Admins to reconcile attendance records, input absence reasons, and record verification audit trails. |
| **Milestone 3: Freshness Camera & Operations Dashboard** | 25 mins | • Created live webcam Freshness Check modal (`FreshnessCheckModal.tsx`) with snapshot capture, timestamp watermark, and diagnostic fallback mode.<br>• Created teacher dashboard attendance widget (`TeacherAttendanceWidget.tsx`) embedded directly on the main dashboard for teachers.<br>• Created centralized Attendance Roster dashboard (`/dashboard/attendance`) with KPI metrics, status filtering, photo preview dialog, and admin verification dialog (`AttendanceRosterTable.tsx`).<br>• Added Attendance navigation link to `AppSidebar.tsx`. |
| **Milestone 3: Verification & Production Build** | 10 mins | • `npx tsc --noEmit` passed with 0 errors.<br>• Next.js production build (`npm run build`) succeeded with all routes compiled.<br>• Dev server running on `http://localhost:3000`. |
| **Milestone 4: Daily Output & Self-Service Requests** | 30 mins | • Added `DailyOutput`, `SwitchRestDayRequest`, and `EarlyTimeOffRequest` models to schema and pushed to Supabase.<br>• Built `submitDailyOutputAction` enforcing 48-hour editing window and announcement policy acknowledgment.<br>• Created Daily Output modal (`DailyOutputModal.tsx`) and admin reporting table (`/dashboard/output`).<br>• Built self-service requests actions (`submitSRDRequestAction`, `submitETORequestAction`, `approveRequestAction`, `rejectRequestAction`).<br>• Created requests portal (`/dashboard/requests`) with teacher submission tabs and manager approval queue.<br>• Integrated navigation links into `AppSidebar.tsx`. |
| **Milestone 5: Announcement & Operational Policy Management** | 20 mins | • Added `announcements:create` and `announcements:manage` permissions to RBAC (`src/lib/auth/rbac.ts`) and seeded them to Admin and Operations Manager roles in `prisma/seed.ts`.<br>• Built announcement server actions (`createAnnouncementAction`, `updateAnnouncementAction`, `toggleAnnouncementStatusAction`, `deleteAnnouncementAction`) in `src/actions/announcements.ts`.<br>• Created `AnnouncementManager.tsx` client component with creation form, live broadcast toggle, and deletion dialog.<br>• Created announcements management page (`/dashboard/announcements`) with permission guards.<br>• Added Announcements navigation link to `AppSidebar.tsx`.<br>• Verified TypeScript compilation (`npx tsc --noEmit` — 0 errors). |
| **Milestone 6: Teacher Resignation, Exit Interview & Monitoring Lifecycle** | 35 mins | • Added `TeacherResignation` model, `ExitInterviewSlot` model, and enums (`ResignationReason`, `ResignationWorkflowStatus`) to `prisma/schema.prisma` and pushed to Supabase PostgreSQL.<br>• Added `resignation:submit`, `resignation:view`, `resignation:manage`, and `exit_interview:manage` permissions to RBAC and seeded role assignments in `prisma/seed.ts`.<br>• Built server actions (`submitTeacherResignationAction`, `bookExitInterviewSlotAction`, `createExitInterviewSlotAction`, `updateResignationStatusAction`, `completeExitInterviewAction`, `completeITClearanceAction`, `deactivateResignedTeacherAction`) in `src/actions/resignation.ts`.<br>• Created teacher self-service resignation view (`/dashboard/resignation`) with `TeacherResignationForm.tsx` featuring standard letter generation (§14), 13 standardized reasons (§13), digital signature, and exit interview slot booking (§15).<br>• Created centralized Resignation Monitoring dashboard (`/dashboard/resignation/monitoring`) with `ResignationMonitoringDashboard.tsx` featuring KPI metrics, timeframe/branch/reason filters, letter viewer dialog, exit interview slot management, IT clearance logging, and one-click teacher deactivation (§16).<br>• Integrated navigation links in `AppSidebar.tsx`.<br>• Verified TypeScript check (`npx tsc --noEmit` — 0 errors). |
| **Milestone 11: Admin & IT Operations, Checklists & Simulations** | 35 mins | • Added `StaffProfile`, `StaffAttendance`, `StaffChecklistResponse`, and `SimulationDrill` models with enums (`StaffRoleType`, `StaffAttendanceStatus`, `SimulationType`, `SimulationStatus`) to `prisma/schema.prisma` and synchronized to Supabase PostgreSQL via `npx prisma db push`.<br>• Added RBAC permissions (`operations:record`, `operations:manage`, `simulations:manage`) and assigned them to ADMIN, OPERATIONS_MANAGER, and IT roles in `prisma/seed.ts`.<br>• Implemented standard checklist definitions for Admin and IT (`checklists.ts`) covering Daily, Weekly, and Monthly task routines (§9.2, §28.1, §29).<br>• Built server actions in `staffOperations.ts`: `recordStaffTimeInAction`, `submitStaffChecklistAction`, `recordStaffTimeOutAction` (with strict lockout enforcement preventing logout until checklist completion per §9.2 & §28.2), and `reconcileStaffAttendanceAction`.<br>• Built simulation server actions in `simulations.ts` including automatic 1st & 3rd Saturday schedule slot generator (§32) and drill audit outcome logging.<br>• Created operational UI: Operations Workstation (`/dashboard/operations`), Unified Staff Attendance Roster (`/dashboard/operations/attendance`), and Simulation Drills Hub (`/dashboard/simulations`).<br>• Updated navigation sidebar with Milestone 11 links and footer status chip.<br>• Verified TypeScript check (`npx tsc --noEmit` — 0 errors). |
| **Milestone 12: Centralized Notification Engine & Scheduled Alerts** | 30 mins | • Added `Notification` model with `NotificationType` (`NO_LOGOUT`, `CHECKLIST_INCOMPLETE`, `ATTENDANCE_ALERT`, `SLOT_DEADLINE`, `SIMULATION_REMINDER`, `SYSTEM_NOTICE`) and `NotificationPriority` enums to `prisma/schema.prisma`; pushed to Supabase PostgreSQL.<br>• Added RBAC permissions (`notifications:read`, `notifications:broadcast`, `system:jobs`) to `rbac.ts` and seeded to Admin, Operations Manager, and IT roles in `prisma/seed.ts`.<br>• Built `dispatchNotificationAction` supporting targeted-user, role-scoped (`TEACHER`, `ADMIN`, `IT`, `OPERATIONS_MANAGER`), and system-wide `ALL` broadcasts (§36).<br>• Built `runScheduledAlertsEvaluationAction` — idempotent evaluator scanning for No-Logout sessions, incomplete End-of-Shift checklists, and upcoming Simulation Drills (§44); all alerts deduplicated per-day.<br>• Created `NotificationBell.tsx` — header dropdown with unread red badge counter, 30-second polling, per-item and mark-all-read, and link to Notification Center.<br>• Created `NotificationCenter.tsx` — full management hub with ALL/UNREAD filter tabs, broadcast dispatch modal (target, type, priority, title, body), and admin job runner with structured execution log output.<br>• Created `/dashboard/notifications` page with RBAC-gated broadcast and job-runner access.<br>• Integrated `NotificationBell` into `AppHeader.tsx`; added Notifications nav links to `AppSidebar.tsx` for all roles.<br>• Strict TypeScript check passed with 0 errors (`npx tsc --noEmit`). |
| **Milestone 13: Consolidated Operations & Cut-Off Reports Engine** | 30 mins | • Built semi-monthly cut-off calculation engine (`getCutOffReportAction` in `reports.ts`) supporting 1st–15th, 16th–End of Month, and custom date range filters per §22 & §29–30.<br>• Implemented cross-module aggregation linking `TeacherProfile`, `TeacherAttendance`, and `DailyOutput` (open slots, conducted classes, cancellations, late minutes, absences, SRDs) and calculating estimated absence/no-logout penalties.<br>• Built staff operations performance report (`getStaffOperationsReportAction`) aggregating Admin & IT checklist compliance rates, shift attendance, and simulation drills per §31.<br>• Created interactive Reporting Desk (`/dashboard/reports`) with KPI summary cards, Teacher Cut-Off table, Staff Operations view, and browser-side CSV export generator.<br>• Added RBAC permissions (`reports:view`, `reports:export`) and linked into `AppSidebar.tsx`. |
| **Milestone 14: Audit Logging, System Configuration & Final Hardening** | 35 mins | • Added `AuditLog` and `SystemSetting` models to `prisma/schema.prisma` and synchronized to Supabase PostgreSQL via `npx prisma db push`.<br>• Created reusable audit logger (`src/lib/audit/auditLogger.ts`) capturing actor, action, module, entity type/ID, payload diffs, IP, and user agent per §42.<br>• Built server actions for system settings (`getSystemSettingsAction`, `updateSystemSettingAction` in `settings.ts`) with automated audit log triggers per §4 & §27.<br>• Built audit log queries (`getAuditLogsAction` in `audit.ts`) supporting text search, module, and action filters.<br>• Created System Configuration Desk (`/dashboard/settings`) grouping settings by ATTENDANCE, PENALTY, OPERATIONS, and GENERAL with live in-line editing.<br>• Created Centralized Audit Logs Viewer (`/dashboard/audit-logs`) with expandable JSON payload diff viewer.<br>• Updated `AppSidebar.tsx` and `AppHeader.tsx` to Milestone 14 "Production Ready".<br>• Zero TypeScript errors (`npx tsc --noEmit`). Production build 100% clean (`npm run build`, 21 routes compiled). |
| **Branding & Footer Modernization** | 10 mins | • Updated application footer branding and milestone badges across AppSidebar and AppHeader.<br>• Rebranded to "NETSPEAK PORTAL v2.0 Developed by Innov8IT". |
| **System Admin 24/7 Exemption & Attendance Decoupling** | 15 mins | • Decoupled System Administrator from mandatory shift attendance and daily checklists.<br>• Configured 24/7 operational privileges allowing System Admin to supervise anytime across all branches without lockout. |
| **Multi-Branch Hierarchy & Center Admin Architecture** | 35 mins | • Added `Branch` model to `prisma/schema.prisma` with unique codes and status tracking.<br>• Added `branch` field and index (`@@index([branch])`) to `TeacherProfile` and `StaffProfile`.<br>• Created branch utility library (`src/lib/branches.ts`) with initial 6 branches (Atimonan, Lopez, Sto. Tomas, Mauban, San Pablo, Gumaca), validation, and normalizers.<br>• Prepared infrastructure for future dynamic branch additions. |
| **Database Sync & Multi-Branch Admin Account Provisioning** | 25 mins | • Synchronized Prisma schema to Supabase PostgreSQL (`npx prisma db push`).<br>• Regenerated Prisma Client bindings (`npx prisma generate`).<br>• Updated `prisma/seed.ts` to provision initial branches and dedicated Center Admin accounts (`admin_atimonan`, `admin_lopez`, `admin_stotomas`, `admin_mauban`, `admin_sanpablo`, `admin_gumaca`) with ADMIN roles and branch-linked `StaffProfile` records. |
| **Universal Security Credentials Standardization** | 10 mins | • Standardized universal initial administrative password to `Netspeak2026!` across System Administrator (`admin`) and all 6 Branch Admins.<br>• Re-seeded Supabase database (`npx tsx prisma/seed.ts`) successfully updating all user password hashes with bcrypt. |
| **Multi-Branch Operational Scoping & Management UI** | 30 mins | • Integrated branch resolution into `AuthenticatedUser` and `getCurrentUser` session loader via `StaffProfile` and `TeacherProfile`.<br>• Created `BranchSwitcher` component in `AppHeader` allowing System Admin to seamlessly switch views between all branches or specific centers.<br>• Center Admins and staff are locked and badged to their assigned branch center.<br>• Scoped `TeacherManagement` and `AttendanceRoster` page queries dynamically with `resolveBranchFilter`.<br>• Added dedicated Branch Management tab in System Configuration Desk (`/dashboard/settings`) for registering expansion branches with real-time audit logging and activation toggles.<br>• Zero TypeScript errors (`npx tsc --noEmit`). |
| **Client/Server Boundary Hardening & Production Build** | 10 mins | • Decoupled isomorphic `ROLES` and `PERMISSIONS` constants into `src/lib/auth/types.ts` to prevent Client Components from importing `next/headers` via `rbac.ts` / `session.ts`.<br>• Updated `AppHeader.tsx` and `branches.ts` to consume types safely across SSR and client hydration boundaries.<br>• Successfully executed Next.js 16.3 production build (`npm run build`) with all 28 dynamic and static routes compiled with 0 errors. |
| **Teacher Work Shift Management & Manila (PHT) Standardization** | 25 mins | • Seeded 6 standardized ESL work shifts in Asia/Manila (PHT, UTC+8): Morning (08:00–17:00), Mid (10:00–19:00), Afternoon (13:00–22:00), Mid-Afternoon (15:00–00:00), Evening (17:00–02:00), and Graveyard (22:00–07:00).<br>• Built Shift Management server actions in `src/actions/shifts.ts` (`getShiftSchedulesAction`, `createShiftScheduleAction`, `updateShiftScheduleAction`, `deleteShiftScheduleAction`, `assignTeacherShiftAction`) with RBAC enforcement.<br>• Created Shift Management desk at `/dashboard/shifts` (`ShiftManagementClient.tsx`) with real-time shift cards, teacher counters, 12-hour/24-hour PHT labels, and create/edit modal.<br>• Enhanced Teacher Profile Detail (`TeacherProfileDetail.tsx`) with interactive Shift Assignment card allowing Center and System Admins to set or change a teacher's scheduled working shift.<br>• Added Shift indicator column to Teacher Directory table (`TeacherDirectoryTable.tsx`) for immediate operational visibility.<br>• Added Shift Schedules navigation link to `AppSidebar.tsx` gated by `attendance:manage` permission.<br>• Strict TypeScript check (`npx tsc --noEmit`) passed with 0 errors. |








